# Only needed to run the graph against a real model.
# The test suite and the eval runner work without it (see README → Quickstart).
ANTHROPIC_API_KEY=

# Optional: tracing. Without these the graph still runs, just untraced.
LANGSMITH_API_KEY=
LANGSMITH_TRACING=true
LANGSMITH_PROJECT=governed-agents
